@model IList<ProductSpecificationModel>
@if (Model.Count > 0)
{
    @foreach (var item in Model)
    {
        <div class="product-specification">
            <span class="label">@item.SpecificationAttributeName</span>
            <span class="value">@Html.Raw(item.ValueRaw)</span>
        </div>
    }
}

